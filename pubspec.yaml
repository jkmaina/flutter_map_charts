name: flutter_map_charts
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  flutter_map_cancellable_tile_provider: ^2.0.0
  get: ^4.6.6
  latlong2: ^0.9.0
  logger: ^2.0.2+1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1


flutter:
  assets:
    - assets/data/admin1.geojson
    - assets/data/countries.geojson
    - assets/data/test.json